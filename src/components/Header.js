import React, { Component } from "react";

import './Header.scss';

class Header extends Component {
    render() {
        return (
            <header id="header">
                This is my header
            </header>
        );
    }
}

export default Header;